type Props = {};

const NotFound = ({}: Props) => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <h3>404 Not Page Found</h3>
    </div>
  );
};

export default NotFound;
